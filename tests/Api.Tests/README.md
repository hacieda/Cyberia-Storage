# Api.Tests

This project will contain integration tests for the Cyberia Storage HTTP API.

Planned auth tests:

- Register user in Open mode
- Reject duplicate email
- Reject duplicate username
- Reject password confirmation mismatch
- Reject registration when mode is Disabled
- Require activation token in InviteOnly mode
- Reject invalid, used, or expired activation tokens
